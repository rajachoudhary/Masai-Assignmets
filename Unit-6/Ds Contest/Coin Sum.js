function runProgram(input){
    input = input.split("\n")
    let N = +input[0]
    let arr = input[1].split(" ").map(Number)
    arr.sort((a,z)=>a-z)
    let output = [...arr]
    for (let i = 0; i < N; i++) {
        
           
    }
}
runProgram(`3
3 5 2 `)