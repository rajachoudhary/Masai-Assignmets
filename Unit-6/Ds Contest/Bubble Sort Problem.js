function runProgram(input){
    input = input.split("\n")
    let N = +input[0]
    let arr = input[1].split(" ").map(Number)
    for (let i = 0; i < N; i++) {
        
        for()
        
    }
}
runProgram(`5
3 5 0 9 8`)